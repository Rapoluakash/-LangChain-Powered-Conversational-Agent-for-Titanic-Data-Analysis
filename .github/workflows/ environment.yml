name: langtitan
channels:
  - defaults
  - conda-forge
  - pip

dependencies:
  - python=3.10
  - pandas=2.2.2
  - pip
  - pip:
      - openai==0.28.1  # ✅ Compatible with LangChain < 0.2
      - langchain==0.1.17
      - langchain-experimental==0.0.51
      - python-dotenv==1.0.1
      - tqdm==4.66.4
