name: langchain-titanic
channels:
  - defaults
  - conda-forge
dependencies:
  - python=3.10
  - pandas
  - pip
  - pip:
      - openai
      - langchain
      - langchain-experimental
      - python-dotenv
